/**
 * Frontend roles types — re-exports from @leadcrm/shared and adds any
 * frontend-only display extensions.
 * Spec 5 Phase B.
 */
export type {
  PermissionFlags,
  RolePermissionRow,
  RoleListItem,
  RoleDetail,
  AssignedUser,
  ResolvedPermissions,
  PermissionAction,
  PermissionModuleDefinition,
} from '@leadcrm/shared';
