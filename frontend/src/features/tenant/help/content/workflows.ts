import { articleFactory, details, steps } from './types';
const a = articleFactory('automation');
export const workflowArticles = [
  a('what-is-workflow', 'What is a Workflow?', 'Respond to supported CRM events with conditions and actions.', [details('The workflow model', 'Trigger → optional conditions → actions.', 'A workflow runs in the environment where its triggering record belongs.', 'Draft or paused workflows do not automatically process new events. Configuration-only mock workspaces do not execute automation.')], { related: ['creating-workflows', 'workflow-triggers', 'environments'] }),
  a('creating-workflows', 'Creating a Workflow', 'Build an automation with the options supported by LeadCRM.', [steps('Open Workflows and select Create workflow.', 'Start from an available recipe or configure your workflow.', 'Enter a name and select a trigger.', 'Add conditions if only some matching records should proceed.', 'Add and configure at least one action.', 'Choose Save draft, then review and test the configuration before activation.')], { notes: ['Workflow creation requires workflow creation access. The available actions depend on the triggering record type.'], related: ['workflow-triggers', 'workflow-conditions', 'workflow-actions'], keywords: ['create', 'automation', 'recipe'] }),
  a('workflow-triggers', 'Workflow Triggers', 'Choose the record event that starts an active workflow.', [details('Supported events', 'Lead created and Lead status changed.', 'Client Profile created and Client Profile status changed (contact records).', 'Deal created, Deal stage changed, Deal closed won, and Deal closed lost.'), details('Choosing an event', 'Choose a trigger that matches the record you want to automate. Its available condition fields and actions update accordingly.', 'Timer-based triggers, email opens, and link clicks are not offered by the current trigger catalog.')], { related: ['workflow-conditions', 'workflow-actions'] }),
  a('workflow-conditions', 'Workflow Conditions', 'Limit which triggering records proceed to actions.', [steps('Choose the trigger first.', 'Add conditions using only the fields offered for that trigger.', 'Choose the condition operator and enter a value of the expected type.', 'Set whether all or any conditions must match, then save and run a dry test.'), details('Fields and operators', 'Deal conditions include title, value, stage, pipeline, priority, and assigned user. Lead/contact conditions include relationship status, source, email, first name, and assigned user.', 'Text comparisons include equals, not equals, contains, not contains, starts with, and ends with. Numeric comparisons require a numeric field.')], { notes: ['Reading relationship status in a condition does not make it an editable workflow action field.'], related: ['workflow-testing', 'workflow-actions'] }),
  a('workflow-actions', 'Workflow Actions', 'Choose supported actions for the triggering record.', [details('Available actions', 'Create task: create an immediate linked follow-up with title, optional description, assignee, due-in-days, and priority.', 'Send notification: notify a workspace user; the owner is the default recipient when not specified.', 'Assign owner: choose a workspace user for the related record.', 'Update safe field: update notes for Client Profiles or description for Leads and Deals. Relationship Status is protected.', 'Move deal stage: choose a target stage for a deal and supply a reason for a lost stage.', 'Send email: use an email template and a connected Gmail sender for a lead or contact.')], { notes: ['External workflow email is blocked in Sandbox. Production email requires a valid connected Gmail sender and recipient.', 'Actions are restricted by record type and validated before activation.'], related: ['activating-workflows', 'workflow-testing', 'email-templates'] }),
  a('activating-workflows', 'Activating a Workflow', 'Enable a valid workflow for future matching events.', [steps('Open the workflow and review its trigger, conditions, and action settings.', 'Resolve missing required action information.', 'Select Save and activate, or use Activate on a valid saved workflow.', 'Use Pause when you want to stop processing future matching events.')], { notes: ['Activation requires workflow editing access. Archived workflows cannot be edited or activated.', 'Activation does not replay all existing records or historical events.'], related: ['workflow-history', 'workflow-testing'] }),
  a('workflow-testing', 'Testing a Workflow', 'Validate a saved configuration without changing records or sending messages.', [steps('Save the workflow and choose Test.', 'Enter an existing sample record ID of the trigger’s record type in the current environment.', 'Select Run dry test.', 'Review the trigger match, condition results, and validation message for each action.')], { notes: ['A dry test validates configuration; it does not replay a historical event or prove external delivery. Testing requires the real backend.'], related: ['workflow-history', 'workflow-troubleshooting'], keywords: ['dry run', 'test', 'sample'] }),
  a('workflow-history', 'Workflow History / Runs', 'Inspect recorded executions and action outcomes.', [steps('Open Workflows and select Runs for the workflow.', 'Select an execution to review its status and steps.', 'Review failed or skipped steps before changing the configuration.')], { notes: ['No runs may mean that no new matching event has occurred since activation. Mock workspaces do not produce real executions.'], related: ['workflow-troubleshooting', 'workflow-testing'] }),
  a('workflow-troubleshooting', 'Troubleshooting Workflows', 'Check environment, activation, conditions, and action requirements.', [steps('Confirm the workflow and sample record are in the same environment.', 'Check that the workflow is active and a supported new event occurred.', 'Review conditions and run a dry test.', 'Inspect Runs for the failed action.', 'For email, check that you are in Live and the selected Gmail sender is connected. For stage movement, check the destination and required lost reason.')], { related: ['workflow-actions', 'workflow-history', 'permission-denied'] }),
];
