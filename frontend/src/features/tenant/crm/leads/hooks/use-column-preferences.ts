/**
 * Re-export from shared hook for backward compatibility.
 * The canonical implementation lives in @/shared/hooks/use-column-preferences.
 */
export { useColumnPreferences } from '@/shared/hooks/use-column-preferences';
