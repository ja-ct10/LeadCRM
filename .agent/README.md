# Universal AgentOS Knowledge Base
This directory is the single source of truth for all AI agents (Kiro, Cursor, Claude Code, etc.).
Do not duplicate rules into IDE-specific configs. Use them only as adapters pointing here.
