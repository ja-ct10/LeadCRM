import { z } from 'zod';
// TODO: define service-orders DTOs and Zod validation schemas
// Pattern: see backend/src/modules/crm/contacts/contacts.dto.ts
export const CreateServiceOrderSchema = z.object({});
